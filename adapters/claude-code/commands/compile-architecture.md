---
description: Compile a project spec into deterministic architecture outputs using the Architecture Compiler skill
---

Read and follow `~/.claude/arch-compiler/skills/compiling-architecture/SKILL.md`.

Use it when the user wants to select architecture patterns, compile a spec, iterate on constraints or NFRs, audit why patterns were selected or rejected, or finalize an architecture for implementation.

If the repo is installed somewhere else, substitute that stable local path before proceeding.

Do not skip the skill's setup and gating steps. Treat the skill file as the authoritative workflow.
