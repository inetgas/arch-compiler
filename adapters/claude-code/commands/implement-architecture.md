---
description: Implement an approved architecture using the Architecture Pattern Implementer skill
---

Read and follow `~/.claude/arch-compiler/skills/implementing-architecture/SKILL.md`.

Use it only when an approved `docs/architecture/` folder exists and implementation work is about to begin.

If the repo is installed somewhere else, substitute that stable local path before proceeding.

Do not skip the pre-flight checks, artifact validation, or approval gates defined in the skill file.
